# ARM udev config — baked into image as default.
# Override at runtime via UDEV_EVENT_TIMEOUT env var.
# Default event_timeout=180 kills rip processes via SIGKILL after 3 minutes.
event_timeout=7200
timeout_signal=SIGTERM
