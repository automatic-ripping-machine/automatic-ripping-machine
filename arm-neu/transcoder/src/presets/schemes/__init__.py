"""
Scheme modules for each GPU variant.

Each submodule (software.py, nvidia.py, intel.py, amd.py) exposes a
module-level ``SCHEME: Scheme`` attribute describing that variant's
capabilities and built-in presets.

"""
